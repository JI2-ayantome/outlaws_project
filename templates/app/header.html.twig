<!-- Navbar -->
<nav class="navbar navbar-expand-lg bg-dark">
    <a class="navbar-brand" href="{{path('app_backend')}}">
        <img src="{{asset('build/images/mjl_logo.png')}}" alt="logo" style = "width:270px; height:70px;">
    </a>
    <button class="navbar-toggler" 
        type="button" 
        data-toggle="collapse" 
        data-target="#navbarSupportedContent" 
        aria-controls="navbarSupportedContent" 
        aria-expanded="false" 
        aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav ml-auto">
            <li class="nav-item">
                <a class="nav-link" href="#">
                    {# Services #}
                </a>
            </li>
            {# <li class="nav-item">
                <a class="nav-link" href="#">
                    Rechercher un document
                </a>
            </li> #}
        </ul>
        <div class="actions-menu">
            {# <a type="button" 
                class="btn btn-secondary" 
                id="signin-button"
                href = "{{ path('app_login') }}">
                Se connecter
            </a> #}
            {# <a type="button" 
                class="btn btn-primary btn-secondary" 
                id="signup-button"
                href = "#" data-toggle="modal" data-target="#dataImportModal">
                Importer Données
            </a> #}
            <a type="button" 
                class="btn btn-link" style = "text-transform: lowercase;" href = "{{path('app_user_profile_action')}}">
                {{ app.user.email }}
            </a>
        </div>
    </div>
</nav>

{# include the modal for importing tha data #}
{% include "app/modals/dataImportModal.html.twig" %}